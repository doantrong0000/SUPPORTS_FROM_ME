<Window x:Class="ProjectApp.ModelFromCad.ScheduleVolumeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectApp.ModelFromCad"
        mc:Ignorable="d"
        Title="ScheduleVolumeView"
        SizeToContent="Height"
        Width="600">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            

            <Style x:Key="NiceButtonStyle" TargetType="Button">
                <!-- Kích thước và font cơ bản -->
                <Setter Property="Height" Value="28" />
                <Setter Property="MinWidth" Value="80" />
                <Setter Property="Padding" Value="10,0" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="FontWeight" Value="SemiBold" />

                <!-- Màu sắc cơ bản -->
                <Setter Property="Background" Value="#FF007ACC" />
                <!-- xanh kiểu VS -->
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="#FF005A9E" />
                <Setter Property="BorderThickness" Value="1" />

                <!-- Hiển thị chuột dạng tay -->
                <Setter Property="Cursor" Value="Hand" />

                <!-- Template -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True" />
                            </Border>

                            <ControlTemplate.Triggers>
                                <!-- Hover -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF1591E5" />
                                </Trigger>

                                <!-- Pressed -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF0060A8" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FF003F70" />
                                </Trigger>

                                <!-- Disabled -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#FFE0E0E0" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FFB0B0B0" />
                                    <Setter Property="Foreground" Value="#FF707070" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
            </Style>

        </ResourceDictionary>
    </FrameworkElement.Resources>

    <StackPanel Margin="6">

        <DataGrid Height="400" x:Name="dgSchedule" AutoGenerateColumns="False"> 
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="300">
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Name"></TextBlock>
                    </DataGridTemplateColumn.Header>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="200">
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Volume (m3)"></TextBlock>
                    </DataGridTemplateColumn.Header>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Volume}"></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal"
                    Margin="0 6 0 0"
                    HorizontalAlignment="Right">
            <Button Content="Close"
                    Click="BtnClose_Click"
                    Style="{StaticResource NiceButtonStyle}">
            </Button>
        </StackPanel>
    </StackPanel>
</Window>